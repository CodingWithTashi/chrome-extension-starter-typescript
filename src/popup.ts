import "./popup.css"

/**
 * on press button in popup.html, handle and send data to background script
 */

/*
btn.addEventListener("click", async () => {
    const response = await chrome.runtime.sendMessage({
        action: "generateSpeech",
        data: { "message": "hi" },
      })
  })
*/

/** clear local storage */
// chrome.storage.local.clear()

/** get local storage */
// await chrome.storage.local.get("selectedVoiceId")

/** set local storage */
//await chrome.storage.local.set({ apiKey: newApiKey })
